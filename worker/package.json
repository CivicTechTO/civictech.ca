{
  "name": "ctt-donations-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.5",
    "wrangler": "^4.87.0"
  },
  "dependencies": {
    "node-addon-api": "^8.7.0",
    "node-gyp": "^12.3.0"
  }
}
